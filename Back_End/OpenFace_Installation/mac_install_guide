# OpenFace 2.2.0 for Mac Installation:
As of now we do not have a bash file to execute this operation. Manual set-up of OpenFace 2.2.0 for MacOS:-

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew update
brew install gcc
brew install boost
brew install tbb
brew install openblas
brew install --build-from-source dlib
brew install wget
brew install opencv

# Clone the OpenFace repository:
git clone https://github.com/TadasBaltrusaitis/OpenFace.git

# Add the following lines in CMakeLists.txt after OpenCV 4 section:
find_package( X11 REQUIRED )
MESSAGE("X11 information:")
MESSAGE("  X11_INCLUDE_DIR: ${X11_INCLUDE_DIR}")
MESSAGE("  X11_LIBRARIES: ${X11_LIBRARIES}")
MESSAGE("  X11_LIBRARY_DIRS: ${X11_LIBRARY_DIRS}")
include_directories( ${X11_INCLUDE_DIR} )

# Install download_models.sh file:
cd OpenFace
bash download_models.sh

# Building:
cd OpenFace
mkdir build
cd build
cmake -D CMAKE_BUILD_TYPE=RELEASE ..  
make

# Test:
cd ..
bin/FaceLandmarkVid -device 0
